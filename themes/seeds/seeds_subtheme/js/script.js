(function ($, Drupal, window, document, undefined) {

  Drupal.behaviors.seeds_subtheme = {
    attach: function (context, settings) {
      // Write your JS here
    }
  }
})(jQuery, Drupal, this, this.document);
