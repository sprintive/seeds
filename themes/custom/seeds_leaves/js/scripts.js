/**
 * @file
 * Seeds Coat custom JS. 
 */
(function ($, Drupal, Bootstrap) {
  Drupal.behaviors.seeds_leaves = {
    attach: function (context) {

    }
  };
})(window.jQuery, window.Drupal, window.Drupal.bootstrap);