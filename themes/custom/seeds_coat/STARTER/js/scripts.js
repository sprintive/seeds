/**
 * @file Behaviors for the THEMENAME theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.BEHAVIOURNAME = {
    attach: function (context, settings) {

    }
  }

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
